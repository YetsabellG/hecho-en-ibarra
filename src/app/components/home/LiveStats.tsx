"use client";
import { useEffect, useState } from "react";
import { supabase } from "../../lib/supabase";
import { Building2, CalendarDays, Package, Tag } from "lucide-react";
export default function LiveStats(){const [stats,setStats]=useState({businesses:1,products:2,events:1,promotions:2});useEffect(()=>{let active=true;async function load(){const [b,p,e,pr]=await Promise.all([supabase.from("businesses").select("id",{count:"exact",head:true}),supabase.from("products").select("id",{count:"exact",head:true}),supabase.from("events").select("id",{count:"exact",head:true}),supabase.from("promotions").select("id",{count:"exact",head:true})]);if(active)setStats({businesses:b.count??1,products:p.count??2,events:e.count??1,promotions:pr.count??2})}void load();return()=>{active=false}},[]);const items=[{icon:Building2,value:stats.businesses,label:"Emprendimiento"},{icon:Package,value:stats.products,label:"Producto"},{icon:CalendarDays,value:stats.events,label:"Evento"},{icon:Tag,value:stats.promotions,label:"Promoción"}];return <section className="border-b border-[#ead6d0] bg-[#fffdf9] py-5"><div className="mx-auto grid max-w-7xl grid-cols-2 gap-3 px-5 sm:grid-cols-4 lg:px-8">{items.map(item=><div key={item.label} className="flex items-center justify-center gap-3 rounded-2xl bg-[#faf0eb] px-4 py-3"><item.icon size={20} className="text-[#891C20]"/><span className="text-xl font-black text-[#32262b]">{item.value}</span><span className="text-xs font-bold text-[#6d5960]">{item.label}{item.value===1?"":"s"}</span></div>)}</div></section>}
