import { getPublishedEvents } from "../../services/event";

export default async function Events() {
  const { data: events } = await getPublishedEvents();
  return <section className="bg-[#F8F5EF] py-20"><div className="mx-auto max-w-7xl px-6"><div className="text-center"><p className="text-sm font-semibold uppercase tracking-[0.25em] text-[#C54B43]">Próximos eventos</p><h2 className="mt-3 text-4xl font-bold sm:text-5xl">Vive Ibarra</h2><p className="mx-auto mt-4 max-w-2xl text-[#766D67]">Ferias, capacitaciones y actividades para impulsar a los emprendedores locales.</p></div>{events && events.length > 0 ? <div className="mt-12 grid gap-7 md:grid-cols-2 xl:grid-cols-3">{events.map((event) => <article key={event.id} className="overflow-hidden rounded-3xl bg-white shadow-sm ring-1 ring-black/5">{event.image && <img src={event.image} alt={event.title} className="h-52 w-full object-cover" />}<div className="p-6"><p className="text-sm font-semibold text-[#C54B43]">{event.event_date || "Fecha por confirmar"}</p><h3 className="mt-2 text-2xl font-bold">{event.title}</h3><p className="mt-3 line-clamp-3 leading-7 text-[#766D67]">{event.description}</p><p className="mt-5 text-sm text-[#766D67]">{event.location || "Ibarra"}{event.event_time ? ` · ${event.event_time}` : ""}</p></div></article>)}</div> : <div className="mx-auto mt-10 max-w-3xl rounded-3xl border border-dashed border-[#D9CFC7] bg-white p-10 text-center text-[#766D67]">Todavía no hay eventos publicados. Cuando exista uno, aparecerá aquí con fecha, lugar e información de inscripción.</div>}</div></section>;
}
