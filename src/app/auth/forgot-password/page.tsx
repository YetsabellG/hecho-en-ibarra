"use client";
import { useState } from "react";
import Link from "next/link";
import { Mail, ArrowLeft } from "lucide-react";
import { requestPasswordReset } from "../../services/auth";

export default function ForgotPasswordPage() { const [email, setEmail] = useState(""); const [sent, setSent] = useState(false); const [loading, setLoading] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); const { error } = await requestPasswordReset(email); setLoading(false); if (error) { alert(error.message); return; } setSent(true); } return <main className="grid min-h-screen place-items-center bg-[#f8f1e7] px-5"><section className="w-full max-w-md rounded-[32px] border border-[#eadbca] bg-[#fffdf9] p-8 text-center shadow-xl sm:p-10"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-[#f4e7d9] text-[#891C20]"><Mail size={26}/></div><h1 className="mt-6 text-3xl font-black">Recuperar contraseña</h1>{sent ? <p className="mt-4 leading-7 text-[#75685f]">Revisa tu correo. Te enviamos un enlace para crear una nueva contraseña.</p> : <><p className="mt-4 leading-7 text-[#75685f]">Escribe el correo con el que te registraste y te enviaremos un enlace.</p><form onSubmit={submit} className="mt-7 space-y-4"><input required type="email" placeholder="correo@ejemplo.com" value={email} onChange={(e) => setEmail(e.target.value)} className="w-full rounded-2xl border px-5 py-4"/><button disabled={loading} className="w-full rounded-2xl bg-[#891C20] py-4 font-bold text-white hover:bg-[#6f1519]">{loading ? "Enviando…" : "Enviar enlace"}</button></form></>}<Link href="/auth/login" className="mt-7 flex items-center justify-center gap-2 text-sm font-bold text-[#6f1519]"><ArrowLeft size={16}/> Volver a iniciar sesión</Link></section></main>; }
