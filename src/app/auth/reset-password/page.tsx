"use client";
import { useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { updatePassword } from "../../services/auth";

export default function ResetPasswordPage() { const [password, setPassword] = useState(""); const [confirm, setConfirm] = useState(""); const [loading, setLoading] = useState(false); const router = useRouter(); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (password !== confirm) { alert("Las contraseñas no coinciden."); return; } setLoading(true); const { error } = await updatePassword(password); setLoading(false); if (error) { alert(error.message); return; } alert("Contraseña actualizada correctamente."); router.push("/auth/login"); } return <main className="grid min-h-screen place-items-center bg-[#f8f1e7] px-5"><section className="w-full max-w-md rounded-[32px] border border-[#eadbca] bg-[#fffdf9] p-8 shadow-xl sm:p-10"><h1 className="text-3xl font-black">Crea una contraseña nueva</h1><p className="mt-3 text-[#75685f]">Usa al menos seis caracteres y guárdala en un lugar seguro.</p><form onSubmit={submit} className="mt-7 space-y-4"><input required minLength={6} type="password" placeholder="Nueva contraseña" value={password} onChange={(e) => setPassword(e.target.value)} className="w-full rounded-2xl border px-5 py-4"/><input required minLength={6} type="password" placeholder="Repite la contraseña" value={confirm} onChange={(e) => setConfirm(e.target.value)} className="w-full rounded-2xl border px-5 py-4"/><button disabled={loading} className="w-full rounded-2xl bg-[#76263d] py-4 font-bold text-white hover:bg-[#5a1d2e]">{loading ? "Guardando…" : "Guardar contraseña"}</button></form><Link href="/auth/login" className="mt-6 block text-center text-sm font-bold text-[#5a1d2e]">Volver al acceso</Link></section></main>; }
